# Razorpay AI Revenue Recovery — Python Dependencies
# Track 3: AI Revenue Recovery
#
# Phase 1 (stdlib only):
#   csv, collections, unittest, hashlib, hmac, json, urllib
#
# Phase 2 (webhook receiver + future dashboard):
#   flask — lightweight HTTP API for webhook endpoint and dashboard

flask>=3.0
